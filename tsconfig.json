{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist", "node_modules", "public/js/**"],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "~/*": ["./src/*"]
    },
    "jsx": "react-jsx",
    "jsxImportSource": "react",
    "// types": "Empty array keeps @types/node globals (Buffer, process, NodeJS.*) out of this browser-first project. Server-side files import what they need explicitly.",
    "types": []
  }
}
